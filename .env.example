# Just the base domain — NO /api suffix, NO trailing slash
# The code adds /api automatically

# Local dev:
VITE_API_URL=http://localhost:5000

# Production (set this in Vercel/Netlify environment variables):
# VITE_API_URL=https://authflow-backend-xyz.onrender.com
